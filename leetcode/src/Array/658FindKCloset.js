


/*
* 二分法：1. ...M...N
* 找出k个x
* */
var findClosestElements = function(arr, k, x) {

    for(let i = 0; i < arr.length; i++){
        let num = arr[i] - x;
        let sorNum = num.sort = (a,b) => a-b
    }
};
